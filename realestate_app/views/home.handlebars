<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="/output.css" rel="stylesheet">
  <title>Home</title>
</head>

<body class="bg-teal-100">
  <div class="flex">
    {{!-- Sidebar --}}
    <aside class="w-full lg:w-1/6 bg-white p-4 mb-4 lg:mb-0 shadow-lg">
      <h2 class="text-xl font-bold mb-4">Locations</h2>
      <ul class="space-y-2">
        {{!-- Suburbs --}}
        <a href="#" class="text-blue-500 hover:underline">Location 1</a>
        <a href="#" class="text-blue-500 hover:underline">Location 2</a>
        <a href="#" class="text-blue-500 hover:underline">Location 3</a>
        {{!-- Can add more if needed --}}
      </ul>
    </aside>

    {{!-- Property grid --}}
    <main class="w-full lg:w-5/6 p-4 lg:p-6">
      <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-5 lg:grid-cols-7 gap-4">
        {{#each properties}}
        <div class="bg-gray-800 text-white rounded-lg shadow-md overflow-hidden"> 
          <div class="p-2 text-lg font-semibold sm:text-base md:text-lg lg:text-xl">{{address}}</div> <!-- Address -->
          <img src="{{image_url}}" alt="Image of {{address}}" class="w-full h-32 object-cover"> <!-- Image -->
          <div class="p-2 text-white font-bold text-lg sm:text-sm md:text-lg lg:text-xl text-center">${{list_price}}</div> <!-- Price -->
        </div>
        {{/each}}
      </div>
    </main>
  </div>

</body>

</html>
