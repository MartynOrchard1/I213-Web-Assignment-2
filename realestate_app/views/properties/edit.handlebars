<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/edit.css">
  <title>Edit Existing Property</title>
</head>

<form action="/properties/{{property.id}}/update" method="post">
    <input type="text" name="address" value="{{property.address}}">
    <input type="text" name="list_price" value="{{property.list_price}}">
    <input type="text" name="suburb" value="{{property.suburb}}">

    {{!-- Insert Current Image Here --}}
    
    <p>Upload New Image (optional)</p>
    <input type="file" name="image" value="{{property.image_name}}">
    <button type="submit"></button>
</form>